[package]
name = "sub-recv"
version = "0.1.0"
edition = "2021"
description = "The live test's receiving side: subscribes from the start, reassembles the fmp4 to a file"

[dependencies]
moq-core = { path = "../../core" }
moq-net = "0.2.15"
tokio = { version = "1", default-features = false, features = ["rt", "sync", "time", "macros"] }
serde_json = "1"
